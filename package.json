{
  "name": "regroove-m4l",
  "version": "2.1.1",
  "description": "Max for Live MIDI device for generating expressive rhythm sequences",
  "main": "code/regroove.js",
  "scripts": {
    "prettier": "npx prettier --write src",
    "postinstall": "./post-install.sh",
    "test": "jest --coverage --verbose -t 'test'",
    "posttest": "prettier --write src"
  },
  "author": "Max Kraan",
  "license": "MIT",
  "dependencies": {
    "mobx": "^6.5.0",
    "regroovejs": "0.2.7"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "jest": "^29.3.1",
    "prettier": "^2.3.2"
  }
}
